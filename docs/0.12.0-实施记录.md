# 0.12.0 实施与验证记录

范围：0.11.1 审查 F01–F10；定制音色；耳机插拔不触发静音调优；咔哒声诊断与可确认的软件不连续修复。

约束：保留已经实测有效的共享低延迟架构，不加入独占输出；不把采样率/位深直接认定为噪声原因；不把编译成功当成设备验收。

- [x] 音频优化生命周期、独立版本与真实状态、失败回滚。
- [x] 输出切换与声卡/驱动变更分离，不在吹奏或伴奏中后台静音测量。
- [x] MIDI 生产者约束、弯音透传、Release 有效测试。
- [x] 定制音色页、独立 EQ 参数、参数无损往返、自定义方案保存。
- [x] 音频参数平滑、过载/回调超时诊断、真实进度。
- [x] 空音列表/状态保留按实际扫描和程序匹配的边界，不声称未实测的库已完整兼容。
- [ ] GitHub Windows Release 编译门禁。

本地验证：macOS 主程序编译通过；10/10 CTest 通过；51/51 Python 回归检查通过；前端脚本语法与页面结构检查通过。

设备验收留项：用户 Windows/板载声卡及 SWAM/空音实际库；44.1/48 kHz；耳机/扬声器；视频＋吹奏＋录音。
