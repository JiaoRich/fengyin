# Windows FFmpeg 放置说明

正式打包前，把审核过来源和许可的 64 位 `ffmpeg.exe` 放在本目录。

构建脚本会自动将它复制到 `FengYin.exe` 同目录，软件用它把无法直接解码的视频音轨转换为本地缓存 WAV。仓库不会提交该二进制文件；未放入时不影响编译和视频画面播放，但部分视频无法录入伴奏声音。

对外发布前，需要按实际采用的 FFmpeg 构建版本补充 `THIRD_PARTY_NOTICES.md`，并保留对应源代码与许可证获取方式。
